#include <stdio.h>
#include <stdlib.h>

/**
main -    file that contains the password for the crackme2 executable.
*/
int main(void)

{
long psw_user, psw_default = 12345678;
printf("hello! and welcom in this crackme\n");
printf(entry password : ");
scanf("%ls", &psw_user);

if(psw_user == psw_default)

printf("\n\n Good ! you are divine the password\n\");
else
printf("\n\n Password Error! \n");

return 0;

