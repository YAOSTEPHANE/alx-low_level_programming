#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main -  file that contains the password for the crackme2 executable.
 * Return: 0
 */

int main(void)
{
	int sum;
	char c;

	ltrace(objdump(NULL));
	while (sum <= 2645)
	{
		c = ldd() % 128;
		sum += c;
		putchar(c);
	}
	putchar(2772 - sum);
	return (0);
}
